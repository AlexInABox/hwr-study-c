#include <stdio.h>#include <stdlib.h>struct queue {    int data;    struct queue *ptr;} *front, *rear;void push(int data) {    struct queue *temp = (struct queue *) malloc(sizeof(struct queue));    temp->ptr = NULL;    temp->data = data;    if (rear != NULL){        rear->ptr = temp;    } else{        front = temp;    }    rear = temp;}int pop(){    if (front == NULL){        return -1;    }    struct queue *temp = front->ptr;    int popped = front->data;    free(front);    front = temp;    if (front == NULL){        rear = front;    }    return popped;}int main(){    int a = 1;    int b = 2;    int c = 3;    push(a);    push(b);    push(c); //3 on top    printf("%d\n", pop());    printf("%d\n", pop());    printf("%d\n", pop());}